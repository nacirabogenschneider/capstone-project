import React from 'react'
import { action } from '@storybook/addon-actions'
import { Maps } from '../Maps'

export default {
  title: 'Maps/Google Maps API',
  component: Maps,
}
export const googleMaps = () => <Maps></Maps>
